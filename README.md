# mnist-cnn-digit-classifier
An interactive computer vision project that classifies handwritten digits (0–9) using a 2-block Convolutional Neural Network trained on the MNIST dataset. Features an end-to-end training pipeline, visual error analysis, and a lightweight Streamlit interface for live image uploads and predictions.
